lwc_severity_count=$(awk -F',' 'NR>1 {
  # Ensure quoted strings don't break the indexing
  severity = $3 + 0;
  file = $5;
  gsub(/"/, "", severity); 
  gsub(/"/, "", file);
  if (severity >= 2 && file ~ /\.js$/) count++;
} END { print count }' "$REPORT_FILE")
