# code-analyzer.yml

# rules:
#   AvoidHardcodingId:
#     tags:
#       - MyScan

#   ClassWithTooManyFields:
#     tags:
#       - MyScan

engines:
  pmd:
    file_extensions: {"apex": [".cls",".trigger"]}
  eslint:
    disable_engine: true
  retire-js:
    disable_engine: true
  regex:
    disable_engine: true
  flow:
    disable_engine: true
  sfge:
    disable_engine: true
  cpd:
    disable_engine: true

rules:
  AvoidDebugStatements:
    engine: pmd
    categories: [Best Practices]

  AvoidHardcodingId:
    engine: pmd
    categories: [Design]












